[package]
name = "classical"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { version = "0.1.0", path = "../common" }

[dev-dependencies]
criterion = "0.7"

[[bench]]
name = "caesar"
harness = false
